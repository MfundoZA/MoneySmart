<Application x:Class="MoneySmart.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MoneySmart"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="Window" TargetType="Window">
                    <Setter Property="Background" Value="#E1E6E1" />
                </Style>
        <Style x:Key="MenuItem" TargetType="MenuItem">
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Height" Value="{Binding Path=ActualHeight, ElementName=mnuMenu}" />
            <Setter Property="Background" Value="#00000000" />
        </Style>
    </Application.Resources>
</Application>

    <!-- Use the appTheme value retrived from the database to programtically set the app theme -->
    <!-- Figure out how to make the nested styles to work in the UI -->